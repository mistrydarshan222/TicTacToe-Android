<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment2_connect3">

    <application
        android:allowBackup="true"
        android:icon="@drawable/tictactoe"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tictactoe"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment2_Connect3">
        <activity android:name=".view.ConnectThree" />
        <activity android:name=".view.GameStart" />
        <activity android:name=".view.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
